DUVIDAS 


- o que é o fragment
- Pra que serve o Outlet, não enntendi o funcionamento
- Como funciona     const pendingOnlyFilter = { ...hideCompletedFilter, ...userFilter };
- Como implementar um  Alert no momento que o usuário aperta o botão de excluir ou editar, porem não tem direito 


    <li>
      <input
         type = "checkbox"
         checked = {!!task.isChecked}
         onClick = {()=> onCheckboxClick(task)}
         readOnly
      />
      <span>{task.text}</span>
      <button onClick={()=> onDeleteClick(task) }>&times;</button>
    </li>